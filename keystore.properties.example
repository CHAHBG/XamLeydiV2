## Example keystore configuration (DO NOT COMMIT REAL SECRETS)
# Copy this file to keystore.properties (which is gitignored) and fill in your values.
# Prefer using environment variables in CI/EAS instead of this file.

storeFile=android/app/release.keystore
storePassword=CHANGE_ME_STRONG_PASSWORD
keyAlias=parcelapp
keyPassword=CHANGE_ME_STRONG_PASSWORD

# After creating the keystore with keytool:
# keytool -genkeypair -v -keystore android/app/release.keystore -alias parcelapp \
#   -keyalg RSA -keysize 4096 -sigalg SHA256withRSA -validity 3650 \
#   -storetype JKS -dname "CN=ParcelApp, OU=Dev, O=Company, L=City, S=State, C=US"

# For Play Store, keep validity >= 25 years is recommended (9125 days). Adjust -validity accordingly.
