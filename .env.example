# Supabase configuration (do NOT commit your real keys)
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key

# Google Maps API key (do NOT commit your real key)
# Used by Expo config via app.config.js
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# When building with EAS, set these as build-time secrets instead of committing to the repo.
# Example (EAS):
# eas secret:create --name REACT_APP_SUPABASE_URL --value "https://..."
# eas secret:create --name REACT_APP_SUPABASE_ANON_KEY --value "..."

# Example (EAS):
# eas secret:create --name GOOGLE_MAPS_API_KEY --value "..."
